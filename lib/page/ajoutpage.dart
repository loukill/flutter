// AjoutPage.dart

import 'package:flutter/material.dart';

class AjoutPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ajouter une fourniture'),
      ),
      body: Center(
        child: Text('Contenu de la page d\'ajout ici'),
      ),
    );
  }
}
